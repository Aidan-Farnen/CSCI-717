this is the recipe reccomender, it will use AI to take an ingredients list and output a reccomended recipe.

to use, open the terminal from this directory and run the below command:
python -m src.app --query "[insert ingredients]" --topk 3